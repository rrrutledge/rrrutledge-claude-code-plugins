{
  "name": "google-docs",
  "private": true,
  "version": "1.0.0",
  "description": "Google Docs API helper scripts (official googleapis client; OAuth via google-auth-library). Deps live here at the plugin root so Claude Code installs them automatically on every plugin install/update; the scripts under skills/google-docs/scripts resolve them via Node's normal upward node_modules walk.",
  "dependencies": {
    "google-auth-library": "^9.15.0",
    "googleapis": "^144.0.0"
  }
}
